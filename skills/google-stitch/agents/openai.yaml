interface:
  display_name: "Google Stitch"
  short_description: "Use Stitch as a design-source adapter"
  default_prompt: "Use $google-stitch to gather the relevant Stitch project and screen context, then hand off cleanly into the shared design implementation or polish workflow."

policy:
  allow_implicit_invocation: true
