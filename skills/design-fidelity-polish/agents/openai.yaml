interface:
  display_name: "Design Fidelity Polish"
  short_description: "Polish a design-driven app to visual parity"
  default_prompt: "Use $design-fidelity-polish to turn an already-implemented design-driven app into a high-fidelity result with exact assets, correct overlays, polished states, and honest QA tracking."

policy:
  allow_implicit_invocation: true
