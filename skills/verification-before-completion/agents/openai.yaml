interface:
  display_name: "Verification Before Completion"
  short_description: "Require fresh evidence before success claims"
  default_prompt: "Use $verification-before-completion to verify the relevant tests, runtime behavior, design expectations, docs, and blockers before claiming the work is done, fixed, or matched."

policy:
  allow_implicit_invocation: true
