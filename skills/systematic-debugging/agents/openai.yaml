interface:
  display_name: "Systematic Debugging"
  short_description: "Root-cause debugging with traceable docs"
  default_prompt: "Use $systematic-debugging to investigate this bug systematically, keep the evidence traceable in docs, and only fix it after the root cause is proven."
